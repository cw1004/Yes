# 처음 시작하기 — 초보자용 단계별 안내

**목표: 5~10분 안에 내 컴퓨터 화면에 차트를 띄웁니다.**

프로그래밍을 몰라도 됩니다. 아래 순서대로 하나씩만 따라 하세요.
설치할 프로그램은 **딱 하나(Node.js)** 뿐이고, 나머지는 복사·붙여넣기입니다.

> 처음 실행하면 **데모 데이터**로 뜹니다. 가짜 시세지만 차트·호가·신호가 전부 진짜처럼 움직여서
> 기능을 익히기에 좋습니다. 실시간 시세는 나중에(선택 단계에서) 켜면 됩니다.

---

## 1단계 · Node.js 설치 (한 번만)

이 앱을 돌리는 엔진입니다.

### 가장 쉬운 방법 — 아래 링크를 누르면 설치 파일이 바로 받아집니다

- **윈도우**: <https://nodejs.org/dist/v24.19.0/node-v24.19.0-x64.msi>
- **맥**: <https://nodejs.org/dist/v24.19.0/node-v24.19.0.pkg>

받은 파일을 실행하고 **계속 / 다음 / 동의**만 누르면 끝입니다. 바꿀 설정은 없습니다.

### 사이트에서 직접 받고 싶다면

1. **https://nodejs.org** 접속
2. 가운데 **`Get Node.js®`** 버튼을 누릅니다
   > 예전에 있던 “LTS” 초록 버튼 두 개는 없어졌습니다. 지금은 이 버튼 하나뿐입니다.
3. 다음 화면 위쪽에 `v24.19.0  LTS` 처럼 **LTS**가 붙어 있는지만 확인하세요 (기본값이 LTS입니다)
4. 아래쪽 **Windows Installer (.msi)** 또는 **macOS Installer (.pkg)** 를 클릭

### 설치가 됐는지 확인하는 법

- **윈도우**: 키보드 `윈도우키` 누르고 `cmd` 입력 → 검은 창(명령 프롬프트) 열기
- **맥**: `command + 스페이스` 누르고 `터미널` 입력 → 엔터

열린 창에 아래를 입력하고 엔터:

```
node -v
```

`v24.19.0` 처럼 **v로 시작하는 숫자**가 나오면 성공입니다. 숫자는 달라도 괜찮습니다.

`명령을 찾을 수 없습니다` 가 나오면 → 창을 완전히 닫고 다시 열어 보세요. 그래도 안 되면 컴퓨터를 재시작하세요.

## 2단계 · 앱 내려받기

1. 아래 주소를 브라우저 주소창에 붙여넣고 엔터 → ZIP 파일이 받아집니다

   ```
   https://github.com/cw1004/Yes/archive/refs/heads/claude/us-stock-scalping-app-3pejru.zip
   ```

2. 받은 ZIP 파일을 **압축 풀기** (윈도우: 우클릭 → 압축 풀기 / 맥: 더블클릭)
3. `Yes-claude-us-stock-scalping-app-3pejru` 라는 폴더가 생깁니다
4. 이 폴더를 **바탕화면**으로 옮기세요 (찾기 쉽게)

이름이 길어서 불편하면 폴더 이름을 `scalpdesk` 처럼 짧게 바꿔도 됩니다.

---

## 이미 예전에 받으셨다면 — 새 버전으로 바꾸기

앱이 계속 좋아지고 있어서, 예전에 받으신 폴더에는 새 기능(🎮 연습, 🔬 실전 준비, 거래 시간대 등)이 없습니다.
**같은 주소로 다시 받으면** 됩니다.

> ⚠️ **먼저 확인!** 예전 폴더에 `.env` 파일(증권사 키·AI 키를 적어 둔 것)을 만드셨다면,
> 새 폴더로 **복사해 오셔야** 합니다. 안 그러면 키를 다시 넣어야 합니다.

1. 앱이 켜져 있으면 **끄세요** (검은 창을 닫으면 됩니다)
2. 위 2단계의 ZIP 주소를 다시 열어 새로 받습니다
3. 압축을 풀면 새 폴더가 생깁니다
4. **예전 폴더의 `.env` 파일을 새 폴더로 복사**합니다 (없으면 건너뛰세요)
5. 예전 폴더는 이름을 `scalpdesk-old` 처럼 바꿔 두었다가, 새 것이 잘 돌면 지우세요

> `.env` 파일이 안 보이나요? 숨김 파일이라 그렇습니다.
> **윈도우**: 탐색기 위쪽 `보기` → `숨긴 항목` 체크 · **맥**: 폴더에서 `Command + Shift + .`

---

## 3단계 · 그 폴더에서 검은 창(터미널) 열기

가장 헷갈리는 부분이라 천천히 하세요. **폴더 안에서 창을 여는 것**이 핵심입니다.

### 윈도우

1. 압축 푼 폴더를 **더블클릭해서 안으로 들어갑니다**
   (안에 `README.md`, `package.json`, `server` 폴더가 보여야 맞습니다)
2. 폴더 창 **위쪽 주소칸**을 마우스로 한 번 클릭 (경로가 파랗게 선택됩니다)
3. 거기에 `cmd` 라고 입력하고 엔터

→ 검은 창이 열리고, 맨 앞에 그 폴더 경로가 적혀 있으면 성공입니다.

### 맥

1. 압축 푼 폴더 **안으로 들어갑니다**
2. 폴더 안 빈 공간에서 **마우스 우클릭**
3. **서비스 → 폴더에서 새로운 터미널 열기** 선택

> 그 메뉴가 안 보이면: `시스템 설정 → 키보드 → 키보드 단축키 → 서비스 → 파일 및 폴더`
> 에서 **"폴더에서 새로운 터미널 열기"** 를 체크하세요.

---

## 4단계 · 실행하기

방금 연 창에 아래를 **그대로 복사해서 붙여넣고** 엔터:

```
node server/server.js
```

이런 글자가 나오면 **성공**입니다:

```
  📈  ScalpDesk  http://127.0.0.1:5173
      미국: 실시간(Yahoo Finance) · 실패 시 데모 자동 전환
      한국: 데모(KIS 키 없음)
```

> 커서만 깜빡이고 아무 반응이 없는 게 정상입니다. 이 창은 **앱이 켜져 있는 동안 계속 열어 둬야 합니다.**
> 창을 닫으면 앱이 꺼집니다.

---

## 5단계 · 브라우저에서 열기

크롬이나 엣지를 열고, 주소창에 아래를 입력하고 엔터:

```
http://127.0.0.1:5173
```

검은 배경에 초록·빨강 캔들 차트가 뜨면 **완료입니다.** 🎉

---

## 6단계 · 화면 둘러보기

화면 맨 위에 탭 3개가 있습니다. 눌러서 옮겨 다니세요.

| 탭 | 무엇을 보나요 |
| --- | --- |
| 🇺🇸 **미국** | 미국 주식 차트. 1분봉~일봉, 이동평균선·볼린저밴드·추세선 |
| 🇰🇷 **한국** | 한국 주식 초단타. **10초봉**, 호가창, 체결 내역 |
| 🧠 **AI 추천** | 오늘의 추천 종목 3개 + 실시간 단타 후보 |
| 🎮 **연습** | 지나간 장으로 돈 없이 매매 연습 |

**처음 해볼 것 3가지**

1. 미국 화면에서 차트 위에 마우스를 올려 보세요 → 아래에 그 봉의 시가·고가·저가·종가가 뜹니다
2. 마우스 **휠을 굴려** 확대/축소, **드래그**해서 좌우 이동, **더블클릭**하면 원래대로
3. 한국 화면에서 `10초` 버튼을 누르고 **1분쯤 기다려 보세요** → 봉이 하나씩 생기는 게 보입니다

> **왜 기다려야 하나요?** 10초봉은 원래 증권사가 주지 않습니다. 이 앱이 실시간 체결을 받아
> 직접 만들기 때문에 **켠 순간부터** 쌓입니다. 지표가 제대로 나오려면 5분 정도 필요합니다.

### ⚡ 종목을 고르기 어렵다면 — 실시간 단타 후보

🧠 **AI 추천** 탭 아래쪽으로 내리면 **실시간 단타 후보** 라는 칸이 있습니다.
오른쪽 위 `자동 스캔` 을 체크하면, 앱이 **미국·한국 종목을 계속 다시 훑어서**
지금 단타를 하기 좋은 순서로 줄을 세워 줍니다. AI를 부르지 않으니 **돈이 들지 않습니다.**

왼쪽에 크게 적힌 숫자가 **단타 적합도(0~100점)** 입니다. 오를지 내릴지가 아니라
**"지금 이 종목으로 단타를 할 만한가"** 를 매긴 점수입니다.

| 등급 | 점수 | 뜻 |
| --- | --- | --- |
| **A** | 75점 이상 | 지금 바로 볼 만함 |
| **B** | 60점 이상 | 괜찮음 |
| **C** | 45점 이상 | 지켜보기 |
| **D** | 그 이하 | 단타에는 부적합 |

- `매수` / `매도` 는 방향입니다. `NEW` 는 방금 새로 올라온 종목이라는 뜻입니다.
- 줄을 **누르면 그 종목 차트로 바로 이동**합니다.
- 그 아래 작은 글씨는 왜 이 점수가 나왔는지입니다 (예: `거래량 평소의 1.7배 · 변동성 ATR 0.8% (적정)`).

> ⚠️ 점수가 높다고 오른다는 뜻이 아닙니다. **"움직임이 있어서 단타를 시도해 볼 만한 종목"** 이라는
> 뜻일 뿐입니다. 사고파는 결정은 반드시 직접 하세요.

---

## 앱 끄는 법 / 다시 켜는 법

### 가장 확실한 방법 — 창을 그냥 닫기

**검은 창 오른쪽 위 `✕` 를 눌러 창을 닫으면 앱이 꺼집니다.** 이게 제일 쉽고 확실합니다.

### 키보드로 끄기 — 안 될 때 확인할 것

`Ctrl` 을 누른 채 `C` 를 눌러도 안 꺼진다면 차례로 확인하세요.

1. **검은 창을 먼저 한 번 클릭하세요.** 메모장이나 브라우저를 보고 있으면 그 창에 입력됩니다.
   검은 창이 **맨 앞에 와 있어야** 합니다.
2. **(윈도우) 창 안에 글자가 파랗게 선택되어 있나요?** 명령 프롬프트는 마우스로 창 안을
   클릭·드래그하면 "선택 모드"가 되는데, 이때 `Ctrl+C` 는 **복사**가 됩니다.
   `Esc` 를 한 번 누른 뒤 다시 시도하세요.
3. 한/영 키가 **한글**로 되어 있으면 영문으로 바꾸고 다시 눌러 보세요.

### 그래도 안 꺼질 때 — 강제로 끄기

새 검은 창을 하나 열고 (폴더는 아무 데나 상관없습니다) 입력하세요.

- **윈도우**
  ```
  taskkill /F /IM node.exe
  ```
- **맥**
  ```
  killall node
  ```

실행 중인 앱이 모두 종료됩니다. `프로세스를 찾을 수 없습니다` 라고 나오면 이미 꺼져 있다는 뜻입니다.

### 꺼졌는지 확인하는 법

브라우저에서 `http://127.0.0.1:5173` 을 새로고침했을 때 **"연결할 수 없음"** 이 나오면 꺼진 것입니다.

### 다시 켜기

3단계(폴더에서 창 열기) → 4단계(`node server/server.js`) 를 반복하면 됩니다.

---

## 📱 앱처럼 쓰기

### 바탕화면 앱으로 만들기 (10초)

브라우저 주소창 없이 아이콘으로 바로 여는 창을 만들 수 있습니다.

1. 크롬이나 엣지로 `http://127.0.0.1:5173` 을 엽니다
2. 주소창 오른쪽 끝의 **설치 아이콘**(모니터에 ⬇ 모양)을 누릅니다
   - 안 보이면 메뉴 `⋮` → **저장 및 공유** → **페이지를 앱으로 설치**
3. **설치**를 누르면 바탕화면에 아이콘이 생깁니다

> 아이콘을 눌렀을 때 "연결할 수 없음"이 나오면 앱(검은 창)이 꺼져 있는 것입니다.
> 먼저 `npm start` 로 켠 뒤 아이콘을 누르세요.

### 휴대폰에서 보기

내 컴퓨터가 켜져 있고, **폰과 컴퓨터가 같은 와이파이**에 있어야 합니다.

**① 컴퓨터에서 "휴대폰 허용" 모드로 실행**

앱을 켤 때 쓰던 그 **검은 창**에서, `npm start` 대신 이걸 칩니다.
윈도우·맥 똑같습니다.

```
npm run mobile
```

> 앱이 이미 켜져 있으면 **먼저 끄세요.** 검은 창을 닫았다가 다시 열면 됩니다.

검은 창에 폰으로 접속할 주소가 나옵니다:

```
  📱  휴대폰에서 보려면 (같은 와이파이에 연결한 뒤 아래 주소로 접속)
      http://192.168.0.12:5173
```

**② 폰 브라우저 주소창에 그 주소를 입력** — 숫자는 사람마다 다릅니다.

**③ 홈 화면에 추가**

- **아이폰(사파리)**: 아래 **공유** 버튼 → **홈 화면에 추가**
- **안드로이드(크롬)**: 메뉴 `⋮` → **홈 화면에 추가**

> ⚠️ **폰에서는 차트만 보입니다.** 주문과 AI 분석은 안전을 위해 막혀 있습니다.
> 폰에서도 쓰려면 `.env` 에 `KIS_UI_TOKEN=원하는_암호` 를 넣고,
> 폰 주소 뒤에 `?token=원하는_암호` 를 붙이세요.
> **같은 와이파이를 쓰는 사람이면 누구나 이 주소로 들어올 수 있습니다.**

**안 열릴 때**
1. 컴퓨터에서 방화벽 허용 창이 뜨면 **허용**
2. 폰이 **모바일 데이터**가 아니라 **같은 와이파이**에 연결됐는지 확인
3. 컴퓨터가 꺼지거나 앱을 끄면 폰에서도 안 됩니다

---
---

# 여기까지가 기본입니다 ✅

아래는 **선택 사항**입니다. 안 해도 앱은 잘 돌아갑니다.
하나씩, 필요할 때만 하세요.

---

## (선택 A) 미국 실시간 시세

**따로 할 게 없습니다.** 인터넷만 되면 자동으로 실시간(약 15분 지연) 시세가 들어옵니다.

화면 왼쪽 위에 `데모 데이터` 라고 노랗게 떠 있다면 시세를 못 가져온 것입니다.
회사 네트워크나 방화벽이 막는 경우가 많으니, 다른 인터넷(개인 와이파이)에서 해보세요.

---

## (선택 B) 한국 실시간 시세 — 한국투자증권 키 발급

한국 화면을 실제 시세로 쓰려면 증권사 키가 필요합니다.

### B-1. 키 발급받기

1. **한국투자증권 계좌**가 필요합니다 (없으면 앱에서 비대면 개설)
2. https://apiportal.koreainvestment.com 접속 → 로그인
3. **My Page → API 신청** 에서 신청
4. **App Key** 와 **App Secret** 두 개를 받아 메모장에 복사해 둡니다
5. **계좌번호**도 확인 (예: `12345678-01`)

> 🔰 **처음이라면 반드시 "모의투자"부터 신청하세요.** 가짜 돈으로 똑같이 연습할 수 있습니다.

### B-2. 키를 파일에 적어 두기

앱 폴더 안에 **`.env`** 라는 파일을 하나 만들고 아래 내용을 넣습니다.
(파일 만드는 법은 아래 **선택 C**에 자세히 적어 두었습니다)

```
KIS_APP_KEY=여기에_앱키
KIS_APP_SECRET=여기에_앱시크릿
KIS_ACCOUNT=12345678-01
KIS_PAPER=1
```

`KIS_PAPER=1` 은 **모의투자**라는 뜻입니다. 실전 계좌를 쓰려면 `0` 으로 바꾸는데,
**처음에는 절대 바꾸지 마세요.**

저장한 뒤 앱을 다시 실행하면 터미널에 이 줄이 뜹니다:

```
  🔑 .env 에서 설정을 읽었습니다: KIS_APP_KEY, KIS_APP_SECRET, KIS_ACCOUNT, KIS_PAPER
```

화면 왼쪽 위 배지가 `모의투자` 로 바뀌면 성공입니다.

> ⚠️ `.env` 는 **비밀번호가 적힌 메모**와 같습니다. 캡처해서 올리거나 남에게 보내지 마세요.
> (저장소에는 올라가지 않도록 이미 제외해 두었습니다)

---

## (선택 C) AI 추천 켜기

기본 상태에서도 AI 추천 화면은 열리지만, 지표만 보고 3종목을 고릅니다(뉴스 분석 없음).
진짜 뉴스 분석을 켜려면 아래 중 **하나**만 하면 됩니다.

### 방법 1 — Claude (성능이 가장 좋음, 유료)

**① 부품 받기** — 앱 폴더에서 연 창에 입력 (1분 정도, 한 번만)

```
npm install
```

**② 키 발급** — https://console.anthropic.com 로그인 → **API Keys** → **Create Key**
→ `sk-ant-` 로 시작하는 키를 복사

> 키는 **만든 직후 한 번만** 보여 줍니다. 창을 닫기 전에 반드시 복사해 두세요.
> 결제 수단을 등록해야 실제로 동작합니다.

**③ 키를 `.env` 파일에 적어 두기** — 한 번만 해 두면 다음부터는 그냥 실행만 하면 됩니다.

앱 폴더에서 연 창에 입력하면 편집기가 열립니다.

- **윈도우**
  ```
  notepad .env
  ```
  “파일을 찾을 수 없습니다. 새로 만드시겠습니까?” → **예**

- **맥**
  ```
  touch .env && open -e .env
  ```

열린 창에 **이 한 줄만** 적습니다:

```
ANTHROPIC_API_KEY=sk-ant-여기에_복사한_키를_붙여넣기
```

**그리고 저장합니다.** 편집기 맨 위 메뉴에서 **파일 → 저장**(맥은 **File → Save**)을 누르면 됩니다.
창을 닫을 때 "저장할까요?" 하고 물으면 **저장**을 눌러도 됩니다.

> 💡 **키보드로 빠르게 하려면**: `Ctrl` 키를 **누른 채로** `S` 를 한 번 누릅니다 (맥은 `command` 키).
>
> 이 문서에서 `A` + `B` 라고 쓴 것은 **"A를 누른 채로 B를 누르라"**는 뜻입니다.
> 두 키를 동시에 탁 치는 게 아니라, 먼저 누른 키는 계속 누르고 있으면 됩니다.

**④ 앱 다시 실행** — 켜져 있으면 `Ctrl + C` 로 끄고 다시 `node server/server.js`

터미널에 이 줄이 보이면 키가 인식된 것입니다:

```
  🔑 .env 에서 설정을 읽었습니다: ANTHROPIC_API_KEY
```

AI 추천 화면 왼쪽 위 배지도 `엔진 1개 연결됨` 으로 바뀝니다.
한 번 분석에 대략 **150~600원** 들고, 실제 사용 금액이 화면에 표시됩니다.

### 방법 2 — Llama, 내 컴퓨터에서 무료로

1. https://ollama.com 에서 프로그램을 받아 설치
2. 검은 창에 입력 (모델 파일이 커서 시간이 좀 걸립니다)

   ```
   ollama pull llama3.1
   ```

3. 앱을 그냥 평소처럼 실행하면 됩니다 — 자동으로 인식합니다

   ```
   node server/server.js
   ```

돈이 안 들지만 Claude보다 분석 품질이 낮고, 인터넷 검색을 못 해서 앱이 모아 준 뉴스만 봅니다.
**키가 필요 없어 `.env` 파일도 만들 필요가 없습니다.**

### 둘 다 켜면?

AI 추천 화면 위쪽에 엔진 두 개가 표시되고, **둘 다 고른 종목**에 `✓ 2개 엔진 합의` 배지가 붙습니다.
의견이 갈리는 종목도 그대로 보여 주므로 판단에 도움이 됩니다.

---

## 설정 파일 정리

키는 전부 앱 폴더의 **`.env`** 파일 한 곳에 모읍니다. 어떤 항목을 쓸 수 있는지는
같이 들어 있는 **`.env.example`** 파일에 주석으로 적어 두었습니다.

| 넣을 항목 | 무엇 |
| --- | --- |
| `ANTHROPIC_API_KEY` | Claude AI 추천 |
| `LLAMA_BASE_URL` · `LLAMA_MODEL` | Llama 사용 (Ollama는 없어도 자동 인식) |
| `KIS_APP_KEY` · `KIS_APP_SECRET` · `KIS_ACCOUNT` · `KIS_PAPER` | 한국 실시간 시세 |
| `PORT` | 5173 번이 이미 쓰일 때 |

터미널에서 직접 지정한 값이 `.env` 보다 우선합니다.

---

## 문제가 생겼을 때

| 화면에 나온 글자 | 무슨 뜻인가요 | 어떻게 하나요 |
| --- | --- | --- |
| `'node'은(는) 내부 또는 외부 명령...` | Node.js 설치가 안 됐거나 창이 옛날 것 | 검은 창을 닫고 다시 열기 → 안 되면 재시작 |
| `Cannot find module ...server.js` | 폴더가 아닌 다른 곳에서 실행함 | 3단계로 돌아가 **폴더 안에서** 창을 여세요 |
| `Ctrl+C` 를 눌러도 안 꺼짐 | 검은 창이 선택 안 됐거나 창 안 글자가 선택 상태 | 검은 창 클릭 → `Esc` → 다시 시도. 또는 창을 `✕` 로 닫기 |
| `EADDRINUSE` | 5173번 자리를 이미 쓰는 중 | 그 창을 닫거나 `taskkill /F /IM node.exe` (맥 `killall node`) 후 다시 실행 |
| 브라우저에 `연결할 수 없음` | 앱이 꺼져 있음 | 검은 창에 초록 글자가 떠 있는지 확인 |
| 왼쪽 위 `데모 데이터` | 실시간 시세를 못 가져옴 | 정상 동작입니다. 실시간은 선택 A·B 참고 |
| `AI 엔진 없음 · 지표 전용` | AI 키가 없음 | 정상 동작입니다. 켜려면 선택 C 참고 |
| 키를 넣었는데 `🔑` 줄이 안 뜸 | `.env` 가 앱 폴더가 아닌 곳에 있음 | 폴더 안에 `README.md` 와 나란히 있어야 합니다 |
| `.env.txt` 로 저장됨 (윈도우) | 메모장이 확장자를 붙임 | 저장할 때 파일 형식을 **모든 파일**로 바꾸고 이름을 `.env` 로 |

포트를 바꾸고 싶다면 (윈도우 `set` / 맥 `export`):

```
set PORT=5174
node server/server.js
```

---

## (선택 D) 얼마에 팔지, 몇 주를 살지 직접 정하기

한국 화면(🇰🇷) 오른쪽 아래 **자동매매** 칸에서 정합니다. 봉 주기(10초봉이든 10분봉이든)와
**상관없이** 아래 숫자대로 사고팝니다 — 봉이 닫히기를 기다리지 않고 **체결이 일어날 때마다** 확인하기 때문입니다.

### 몇 주를 살지 — `수량 결정`

| 고르는 것 | 뜻 |
| --- | --- |
| **자동 계산(금액·위험)** | 잔고와 `1회 투입(원)`, `1회 위험(%)` 을 보고 앱이 알아서 수량을 정합니다 |
| **직접 입력(주식 수)** | `주식 수` 칸에 적은 만큼 **항상 그 수량으로** 삽니다 |

직접 입력을 골랐어도 **돈이 모자라면 살 수 있는 만큼만** 삽니다. 없는 돈으로 주문하지 않습니다.

### 언제 팔지 — `청산 기준`

| 고르는 것 | 적는 것 | 예 |
| --- | --- | --- |
| **신호 자동(ATR)** | 없음 | 앱이 그날 변동성에 맞춰 알아서 |
| **퍼센트(%)** | 손절 %, 목표 % | 산 가격에서 **-1% 되면 팔고, +1.5% 되면 판다** |
| **금액(원)** | 손절 원, 목표 원 | **3만원 잃으면 팔고, 5만원 벌면 판다** |
| **호가 수** | 손절 호가, 목표 호가 | 3칸 내려가면 팔고, 8칸 올라가면 판다 |

가장 이해하기 쉬운 것은 **퍼센트**나 **금액**입니다. 숫자를 넣으면 실제로 얼마에 팔리는지가
바로 아래 미리보기에 나옵니다.

### 수동으로 산 주식도 지켜 주기

`수동 주문` 칸에서 **`♡ 자동 청산 걸기`** 를 체크하고 매수하면, 직접 산 주식도 위 기준으로
자동 감시합니다. 보유 목록에 손절가·목표가가 같이 표시되고, 시간 제한 없이 계속 지켜봅니다.

> 감시를 풀어도 **주식은 그대로 남아 있습니다.** 감시만 꺼지는 것이지 파는 게 아닙니다.

---

## 💸 오르고 팔았는데 왜 손해죠?

**수수료와 세금 때문입니다.** 주식은 팔 때 증권거래세 **0.20%** 를 뗍니다.
수익이 났든 손해가 났든 **무조건** 뗍니다. 여기에 수수료가 매수·매도 각각 붙습니다.

그래서 **조금 오른 걸로는 본전도 안 됩니다.**

### 10,000원짜리를 1,000주 샀다면

| 얼마에 파나 | 결과 |
| --- | --- |
| 10,020원 (+0.2%) | **767원 손해** 😱 올랐는데 손해입니다 |
| 10,030원 (+0.3%) | 9,212원 이익 |

**이 종목의 본전가는 10,030원입니다.** 앱이 주문창에 이 가격을 직접 띄워 줍니다.
`본전가 10,030원 — 3호가 위 (+0.30%)` 처럼요. **이 가격 아래로는 팔면 손해입니다.**

> 왜 10,025원이 아니라 10,030원일까요? 1만원짜리 주식은 **10원 단위로만** 주문할 수 있습니다.
> 10,025원이라는 가격 자체가 없습니다. 이걸 호가단위라고 합니다.

### ETF는 세금이 없습니다

**KODEX, TIGER 같은 국내 ETF는 증권거래세가 면제**입니다. 위와 같은 조건이면
본전가가 **10,010원(1호가)** 으로 뚝 떨어집니다. 단타는 ETF가 훨씬 유리합니다.
앱이 종목명을 보고 알아서 구분해 계산합니다.

### 내 수수료율이 다르면

기본값은 **한국투자증권 비대면 계좌 우대 요율(0.0036396%)** 입니다.
본인 계좌 요율은 증권사 앱의 수수료 안내에서 확인할 수 있고, 다르면 `.env` 파일에 적으면 됩니다.

```
KR_COMMISSION_RATE=0.00014527
```

> 앱의 보유 목록에 뜨는 손익은 **세금·수수료를 다 뺀 실제 금액**입니다.
> "지금 팔면 손에 남는 돈"이라고 보시면 됩니다.

---

## ⏰ 언제 해야 하나 — 하루 2시간이면 충분합니다

단타는 **거래가 몰리는 시간**에만 유리합니다. 사람이 몰릴 때는 원하는 가격에 잘 사고팔리지만,
한산한 시간에는 같은 방법을 써도 미끄러지기만 합니다.
그래서 **오래 붙어 있을수록 불리합니다.** 짧게 하고 끄는 게 맞습니다.

🧠 **AI 추천** 화면 맨 위에 지금이 어떤 시간인지와 **몇 분 남았는지**가 뜹니다.

### 하루 계획표 (한국 시간)

| 시간 | 시장 | 뭐하는 시간 | |
| --- | --- | --- | --- |
| **09:00~09:40** | 🇰🇷 | **시초가 폭풍** — 하루 중 가장 크게 움직임 | 🥇 1순위 |
| 10:00~11:00 | 🇰🇷 | 기관 1차 매매 — 방향이 정해지는 구간 | 🥉 |
| 11:30~13:30 | 🇰🇷 | 점심 — 거래가 마름 | ❌ **쉬세요** |
| 14:20~15:20 | 🇰🇷 | 마감 물량 — 기관 정리 매매 | 🥈 |
| **22:30~23:30** | 🇺🇸 | **미국 개장** — 미국장에서 가장 큰 시간 | 🥇 1순위 |
| 01:00~02:30 | 🇺🇸 | 미국 점심 | ❌ **쉬세요** |
| 04:00~05:00 | 🇺🇸 | 미국 마감 | 🥈 |

> 미국 시간은 **여름·겨울이 한 시간 다릅니다** (서머타임). 겨울에는 개장이 KST 23:30 입니다.
> 앱이 알아서 계산해서 보여 주니 외우지 않아도 됩니다.

**딱 두 구간만 골라 잡으면 이렇습니다**
- 아침 **09:00~09:40** — 출근 전 40분
- 밤 **22:30~23:30** — 미국 개장 1시간

### 자동매매를 이 시간에만 돌리기

🇰🇷 한국 화면 자동매매 칸의 **`거래 시간대`** 에서 고릅니다.

| 고르는 것 | 뜻 |
| --- | --- |
| 제한 없음 | 장중 내내 (기본값) |
| **주요 구간만** | 점심시간에는 사지 않음 |
| **골든타임만** | 09:00~09:40 에만 삼 |

> **파는 것은 시간과 상관없이 계속 감시합니다.** 산 주식이 시간 때문에 못 팔리는 일은 없습니다.

### 이 표를 믿어야 하나요?

"개장 직후와 마감 직전에 거래가 몰리고 점심에 마른다"는 건 널리 알려진 사실입니다.
하지만 **정확히 몇 %인지는 종목마다 다릅니다.**

그래서 앱은 이 표를 **참고용 가정**으로만 쓰고, 자동 스캔이 켜져 있는 동안
**직접 재서** 막대그래프로 보여 줍니다. 며칠 켜 두면 *내가 보는 종목*의 진짜 골든타임이 드러납니다.
표와 실측이 다르면 **실측이 맞습니다.**

---

## 🎮 돈 없이 먼저 연습하기

맨 위 탭에 **🎮 연습** 이 있습니다. **지나간 장을 다시 틀어 놓고 직접 사고파는** 모드입니다.
가짜 돈이라 잃어도 아무 일 없습니다.

### 어떻게 하나요

1. 종목코드(예: `005930`)와 시작 자금을 고르고 **▶ 연습 시작**
2. **▶ 1봉** 또는 **▶▶ 5봉** 을 눌러 시간을 앞으로 보냅니다 (**⏵ 자동** 을 누르면 알아서 흘러갑니다)
3. 사고 싶을 때 **매수**, 팔고 싶을 때 **매도**
4. 다 했으면 **■ 끝내고 채점**

키보드가 더 편합니다 — `스페이스` 한 봉 넘기기 · `B` 매수 · `S` 매도 · `P` 자동

### 왜 진짜 연습이 되나요

- **다음 봉이 뭔지 볼 수 없습니다.** 앱이 아니라 **서버가 쥐고 있다가 하나씩 열어 줍니다.**
  미래를 알면 연습이 아니니까요.
- **지금 보이는 가격에 못 삽니다.** 버튼을 누르면 **다음 봉 시작 가격**에 체결됩니다.
  실제 매매도 그렇습니다.
- **수수료와 세금이 그대로 빠집니다.** 화면의 평가손익은 **지금 팔면 손에 남는 돈**입니다.

### 채점이 알려 주는 것

끝내고 채점하면 **그냥 사서 가만히 들고 있었을 때**와 나란히 보여 줍니다.

> 돈을 벌었어도 **그냥 들고 있는 것보다 못했다면 잘한 게 아닙니다.**
> 그럴 때 앱은 *"그냥 사서 들고 있었다면 N원이었습니다 — 단타가 손해였습니다"* 라고 말해 줍니다.
> 거래를 너무 자주 해서 수수료로 다 나갔으면 그것도 알려 줍니다.

**실계좌를 연결하기 전에 여기서 충분히 연습해 보세요.** 잃을 게 없습니다.

---

## 🔬 진짜 돈을 넣기 전에

한국 화면(🇰🇷) 자동매매 칸 위에 **🔬 실전 준비** 라는 상자가 있습니다. 버튼이 두 개입니다.

### `실전 점검` — 빠뜨린 게 없는지 확인

비행기가 이륙 전에 체크리스트를 읽는 것과 같습니다. 누르면 9가지를 한 번에 봅니다.

| 표시 | 뜻 |
| --- | --- |
| ✔ | 괜찮습니다 |
| ! | 확인해 보세요 (막지는 않습니다) |
| ✘ | **이건 고쳐야 합니다** |

✘ 가 하나라도 있으면 **실전 준비가 안 된 겁니다.** 무엇을 어떻게 고치면 되는지
바로 아래 화살표(→)로 알려 줍니다.

### `수익 구간 찾기` — 손절·목표를 얼마로 잡을까

혼자 정하기 어려운 숫자입니다. 이 버튼을 누르면 앱이 **과거 봉으로 108가지 조합을 전부
돌려 보고** 어떤 조합이 실제로 돈이 됐는지 색깔 표로 보여 줍니다.

- **파란 칸** = 그 설정으로 돈을 벌었음
- **빨간 칸** = 잃었음
- **흰 테두리** = 앞 기간에서 좋았고 **뒤 기간에서도 다시 확인된 것** ← 이것만 믿을 만합니다

흰 테두리가 있으면 아래에 `이 설정 적용` 버튼이 뜹니다. 누르면 자동매매 설정에 바로 들어갑니다.

> **흰 테두리가 하나도 없으면?** 그게 정상일 때가 많습니다.
> 앱이 *"어떤 설정으로도 안정적인 수익 구간이 확인되지 않았습니다"* 라고 말하면
> **그 종목은 그 기간에 단타로 먹을 게 없었다는 뜻**입니다. 억지로 하지 마세요.

### 왜 앞뒤로 나눠서 보나요?

108개를 돌려서 제일 좋은 걸 고르면, 그건 보통 **과거에 우연히 맞은 것**입니다.
로또 번호 100개를 뽑아 놓고 그중 맞은 걸 고르는 것과 같습니다.

그래서 앱은 데이터를 앞 70% / 뒤 30%로 나눠서, **앞에서 좋았던 걸 뒤에서 다시 시험**합니다.
둘 다 통과한 것만 흰 테두리를 받습니다.

> ⚠️ 그래도 **과거 기록일 뿐입니다.** 미래에도 된다는 뜻이 아닙니다.
> 반드시 모의투자로 먼저 확인하세요.

---

## 🚨 꼭 지켜 주세요

1. **자동매매는 처음부터 켜지 마세요.** 한국 화면의 자동매매는 기본이 꺼짐 + 모의 실행입니다.
   실제 주문이 나가려면 여러 단계를 직접 켜야 하는데, **최소 몇 주는 모의투자로 지켜본 뒤**에 판단하세요.
2. **AI 추천은 정답이 아닙니다.** 최신 뉴스를 읽어도 미래는 알 수 없습니다.
   화면의 `리스크` 항목과 출처 링크를 꼭 함께 읽으세요.
3. **이 앱의 모든 화면은 참고 자료이며 투자 권유가 아닙니다.**
   매매 판단과 그 결과에 대한 책임은 본인에게 있습니다.
4. **키는 비밀번호입니다.** 캡처해서 올리거나 다른 사람에게 보내지 마세요.
5. **`직접 입력(주식 수)` 을 쓸 때는 수량을 다시 한 번 확인하세요.** 이 모드에서는
   `1회 투입(원)` 한도가 적용되지 않습니다. 적은 수량부터 시작하세요.
6. **오래 붙어 있지 마세요.** 단타는 시간을 늘린다고 수익이 늘지 않습니다.
   거래가 몰리는 구간에서 짧게 하고 끄는 편이 낫습니다.
7. **실전 점검에서 ✘ 가 있으면 실계좌로 넘어가지 마세요.** 그리고
   `수익 구간 찾기` 결과가 좋아 보여도 **과거 기록일 뿐**입니다.

---

## 더 알고 싶다면

- [README.md](README.md) — 기능 전체 설명, 지표·신호가 어떻게 계산되는지
- 화면 아래쪽 `스캔한 후보 종목` 표 — AI가 무엇을 보고 골랐는지 전부 공개됩니다
