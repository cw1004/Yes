# RoomCraft AI

**사진 한 장으로 공간을 리모델링하고, 그 안에 배치된 가구를 그대로 수익으로 연결하는 AI 인테리어 스튜디오.**

첨부해 주신 화면 기획을 실제로 동작하는 React 앱으로 구현한 것입니다. Before/After 렌더부터
제휴 링크 생성·템플릿 판매·클라이언트 견적까지, 4개의 수익 모델이 하나의 흐름으로 이어집니다.

---

## 빠른 시작

```bash
cd roomcraft
npm install

npm run server   # 터미널 1 — API 서버 (8787)
npm run dev      # 터미널 2 — 웹 (5173)
```

API 키가 없어도, 서버를 안 띄워도 **전체 흐름이 그대로 동작합니다.**

| 상황 | 동작 |
|---|---|
| 서버 없음 | 목(mock) 렌더 + 로컬 크레딧. 화면 상단에 안내가 뜹니다. |
| 서버만 실행 | 계정·결제·크레딧 원장 동작. 렌더는 여전히 목. |
| 서버 + API 키 | 실제 AI 렌더/챗. 크레딧은 서버가 차감. |
| 서버 + Stripe 키 | 실제 Stripe Checkout. 없으면 dev 결제 시뮬레이터. |

```bash
cp .env.example .env   # 필요한 키만 채우면 됩니다
```

### 테스트

```bash
npm run test:api    # API 통합 + 크레딧 원장 + 링크 추적 (45개)
node smoke.mjs      # 스튜디오 UI (20단계)
node e2e-auth.mjs   # 계정·결제·크레딧 브라우저 E2E (13단계)
node shoptest.mjs   # 쇼퍼블 이미지 태그 (11단계)
node e2e-tracking.mjs  # 클릭 추적·구독 갱신 (8단계)
```

---

## 화면 구성

| 영역 | 설명 |
|---|---|
| **헤더** | 공간 선택, 현재 스타일, 확장형 뷰, 크레딧·플랜, 수익 허브, 무드보드 합계 |
| **캔버스 — Before/After** | 드래그(및 ←/→ 키) 비교 슬라이더, 팔레트 스와치, 가구 핫스팟 태그 |
| **캔버스 — 2D 스테이징** | 평면도 위에 가구 칩을 드래그 배치 (더블클릭으로 제거) |
| **캔버스 — 업로드/스타일** | 사진 업로드(드래그&드롭), 8종 스타일 아키타입, 변환 강도 슬라이더 10~100% |
| **사이드 — AI Designer** | 디자이너 "Archie" 챗, 추천 제품 카드에서 바로 무드보드에 담기 |
| **사이드 — Spec Sheet** | 스타일 큐레이션 가구 목록, 가격·소재·추천 사유, 일괄 Sync |
| **사이드 — Earnings** | 채널별 예상 수수료, 보수/기대/낙관 3단 추정, 수익원 요약 |

---

## 4가지 수익 모델

### 0. 클릭 추적 — 수익 판단의 유일한 근거

앱의 사용자는 **창작자**지만 가구를 사는 사람은 **창작자의 독자**입니다. 수수료는 블로그·카톡으로
나간 링크에서 발생하므로, 그 링크가 추적되지 않으면 어떤 스타일·채널이 돈이 되는지 알 수 없습니다.

로그인 상태에서 내보내면 모든 링크가 `/r/:token` 추적 링크로 발급됩니다.

```
독자가 링크 클릭 → /r/:token → 클릭 기록 → 302 → 실제 쇼핑몰
```

- 같은 (사용자·제품·몰·채널) 조합은 토큰을 재사용해 통계가 흩어지지 않습니다
- 제휴 ID를 바꾸면 기존 토큰의 대상 URL이 갱신됩니다(이미 배포한 링크가 살아있음)
- 대상 호스트 화이트리스트로 **오픈 리디렉터 악용을 차단**합니다
- 원문 IP는 저장하지 않고, 일별 솔트를 섞은 해시로 방문자 중복만 구분합니다
- 기록에 실패해도 리디렉트는 반드시 수행합니다 — 통계 때문에 매출을 잃으면 안 됩니다
- 비로그인이면 원본 딥링크로 폴백합니다. 추적만 빠지고 기능은 그대로입니다

Earnings 패널의 **실측 클릭** 카드에서 채널·제품별 클릭과 방문자 수를 확인합니다.
기대 정산액(가정값)과 실측 클릭을 나란히 보여주어 둘을 혼동하지 않게 했습니다.

### 0-1. 쇼퍼블 이미지 태그 — 링크가 노출되는 지점
렌더가 끝나면 큐레이션 가구가 이미지 위에 태그로 찍힙니다.

- **클릭** → 상품 카드. 활성화된 전 채널 구매 링크가 바로 열립니다(추적 ID 자동 삽입).
- **드래그** → 태그 위치 이동. 렌더마다 가구 배치가 달라 초기 좌표는 추정값이라, 창작자가
  실제 가구 위로 맞출 수 있어야 합니다.
- 태그 추가/삭제, 무드보드 담기, 브랜드 공식몰 이동
- **쇼퍼블 HTML 내보내기** — 이미지와 태그 위치를 그대로 담은 HTML. 블로그에 붙여넣으면
  독자가 이미지의 태그를 눌러 제휴 링크로 이동합니다. 외부 CSS/JS 없이 인라인 스타일만
  사용합니다(블로그 에디터 대부분이 `<style>`/`<script>` 를 제거하므로).

태그는 After 이미지의 상품이라 비교 슬라이더가 Before 쪽을 덮으면 숨겨집니다. 그래서 초기
위치는 슬라이더 기본값(50%) 오른쪽에만 배치합니다.

### 1. 제휴 커머스
**5개 권역 37개 쇼핑몰 / 22개 제휴 프로그램**을 지원합니다.

| 권역 | 쇼핑몰 | 제휴 프로그램 |
|---|---|---|
| 🇰🇷 국내 | 쿠팡, 오늘의집, 네이버쇼핑, 11번가, G마켓, 옥션, SSG닷컴, 롯데온, 한샘몰, 현대리바트 | 쿠팡 파트너스, 오늘의집 파트너스, 링크프라이스, 애드픽 |
| 🇺🇸 미국·글로벌 | Amazon.com, Wayfair, West Elm, Crate & Barrel, Article, Houzz, Lumens, Etsy, eBay | Amazon Associates, Impact, CJ Affiliate, Awin, Rakuten Advertising, eBay Partner Network |
| 🇯🇵 일본 | 楽天市場, Amazon.co.jp, Yahoo!ショッピング, LOWYA, ニトリ, 無印良品 | 楽天アフィリエイト, Amazon アソシエイト, バリューコマース, A8.net |
| 🇨🇳 중국 | 淘宝/天猫, 京东, AliExpress, Temu, 1688 | 淘宝联盟, 京东联盟, AliExpress Portals, Temu Affiliate |
| 🇪🇺 유럽 | Amazon.de, IKEA, Nordic Nest, Connox, Made in Design, Westwing, Maisons du Monde | Amazon PartnerNet, Awin, Impact, Tradedoubler |

**모델이 2계층인 이유:** 국내 종합몰 다수가 링크프라이스 한 계정으로 묶이고, 유럽 브랜드
다수가 Tradedoubler로 묶입니다. 몰마다 ID를 받는 구조로는 확장되지 않아
`AffiliateProgram`(가입 주체)과 `Mall`(링크 대상)을 분리했습니다. 가입한 프로그램의 ID만
넣으면 그 프로그램에 속한 모든 몰 링크에 자동 삽입됩니다.

채널은 개별로 켜고 끌 수 있고, 켠 채널만 CSV 열·기대 정산액·딥링크 생성에 반영됩니다.

- **블로그 포스팅용 HTML** — 티스토리/네이버 에디터에 그대로 붙여넣기 (대가성 문구 자동 포함)
- **카톡 공유용 텍스트** — 인스타 프로필·유튜브 설명란용 단문
- **CSV** — 정산·스프레드시트 관리용
- **실시간 딥링크 생성기** — 카탈로그에 없는 제품도 이름만 넣으면 활성 채널 전체 링크 생성
- **전환율 가정 슬라이더** — 기대 정산액 = 상한 × 전환율(기본 2%). 상한을 수익으로 착각하지
  않도록 두 값을 분리해 보여줍니다.

### 2. 템플릿 마켓 판매
현재 스타일·강도·큐레이션 구성을 하나의 프리셋으로 묶어 판매 등록. 플랜에 따라 70~90% 정산.

### 3. 클라이언트 납품 견적서
가구 마진율·디자인 피·부가세를 설정하면 원가/청구액/**디자이너 순이익**이 자동 계산됩니다.
텍스트 복사 및 파일 저장 지원.

### 4. 구독 & 크레딧
Free / Creator($19) / Pro Creator($49) / Studio($149) 4개 플랜 + 크레딧 팩.
렌더 1회 = 5크레딧, 챗 1턴 = 1크레딧.

**크레딧은 서버 권한입니다.** 로그인하면 잔액의 진실은 서버에 있고 클라이언트는 표시만 합니다.

---

## 계정 · 결제 · 크레딧 (서버)

### 크레딧을 원장(ledger)으로 둔 이유
잔액을 컬럼 하나로 들고 있으면 동시 요청에서 값이 덮이고, 이중 지급을 사후에 추적할 수 없습니다.
`credit_ledger` 에 모든 변동을 append 하고 **잔액 = SUM(delta)** 로 계산합니다.

- 차감은 잔액 확인과 같은 트랜잭션 안에서 → 동시 요청이 잔액을 음수로 만들 수 없음
- `(reason, ref)` 유니크 인덱스 → 같은 결제/요청으로 두 번 지급 불가
- 렌더 실패 시 자동 환불 기록이 남음 (`render:refund`)
- 계정 모달에서 전체 원장을 확인 가능

### 결제 흐름
```
클라이언트 → POST /api/payments/checkout   payments 행 생성 (pending)
                    ↓
      Stripe Checkout  또는  dev 시뮬레이터
                    ↓
Stripe 웹훅 → fulfillPayment(paymentId)
              UPDATE ... WHERE status='pending'  ← 0행이면 이미 처리됨
              플랜 반영 + 원장에 크레딧 적립
```

지급은 두 프로바이더가 **같은 `fulfillPayment` 함수**를 씁니다. 토스페이먼츠 등을 추가할 때는
체크아웃 생성과 웹훅 검증만 붙이면 됩니다.

- 웹훅 서명 검증 실패는 **반드시 400 으로 거절**합니다. 여기를 통과시키면 누구나 크레딧을 받습니다.
- `/api/payments/dev/complete` 는 Stripe 키가 설정되면 **자동으로 비활성화**됩니다.

### 계정
- 비밀번호: `scrypt` (사용자별 salt, node:crypto — 외부 의존성 없음)
- 세션: DB 저장 토큰 + httpOnly 쿠키. JWT 와 달리 즉시 폐기 가능
- 로그인 실패 메시지는 계정 존재 여부를 흘리지 않도록 통일

### API

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/api/health` | 서버·AI·결제 프로바이더 상태 |
| POST | `/api/auth/signup` `/login` `/logout` | 계정 |
| GET | `/api/auth/me` | 현재 사용자 + 잔액 |
| POST | `/api/payments/checkout` | 결제 세션 생성 |
| POST | `/api/payments/webhook` | Stripe 웹훅 (raw body, 서명 검증) |
| POST | `/api/payments/dev/complete` | dev 전용 결제 완료 |
| GET | `/api/payments/history` | 결제 내역 |
| GET | `/api/credits/ledger` | 크레딧 원장 |
| POST | `/api/render` `/api/chat` | AI 프록시 (크레딧 차감) |
| GET/PUT | `/api/sync/state` | 스튜디오 상태 동기화 |
| GET/POST/DELETE | `/api/sync/templates` | 템플릿 마켓 |

---

## 아키텍처

```
src/
  data/          스타일 8종 · 가구 카탈로그 30종 · 공간 8종 · 플랜/크레딧
  lib/
    affiliate.ts   몰별 딥링크 생성 + 커미션 추정
    prompt.ts      강도(10~100%)를 렌더 지시문으로 번역
    quote.ts       견적/마진/순이익 계산
    exporters.ts   CSV · 블로그 HTML · 카톡 텍스트 · 클립보드
    sampleRoom.ts  네트워크 없이 쓰는 샘플 Before 이미지 생성
    ai/            서버 프로바이더 ↔ 목 프로바이더 폴백
  store/useStudio.ts   전역 상태 (zustand + localStorage 영속화)
  components/    헤더 · 캔버스 · 사이드패널 · 수익화 모달 4탭 · 무드보드 모달
    BeforeAfter.tsx    비교 슬라이더 + 쇼퍼블 태그 (포인터 캡처)
    ShoppableTag.tsx   태그 클릭 시 뜨는 상품·구매링크 카드
    TagManager.tsx     태그 추가/삭제 + 쇼퍼블 HTML 내보내기
  lib/api.ts     서버 API 클라이언트 (쿠키 세션)
  store/useAuth.ts     계정·결제 상태

server/
  index.js       라우터 조립
  db.js          SQLite 스키마 + 마이그레이션
  auth.js        계정·세션 (scrypt + httpOnly 쿠키)
  credits.js     크레딧 원장 — 잔액의 유일한 권한
  payments.js    Stripe Checkout + dev 시뮬레이터 + 웹훅
  ai.js          렌더/챗 프록시 (크레딧 차감 + 실패 시 환불)
  sync.js        스튜디오 상태·템플릿 저장
  links.js       제휴 링크 발급 + /r/:id 클릭 추적
  api.test.mjs   API 통합 테스트
```

**영속화 정책:** 렌더 결과와 원본 사진은 data URL이라 용량이 커서 localStorage 쿼터를 넘길 수
있습니다. 무드보드·제휴 설정·견적·플랜·크레딧 등 세션 간 유지가 필요한 상거래 상태만 저장합니다.

---

## 실제 서비스로 내보내기 전에 필요한 것

이 저장소는 **프론트엔드 제품 전체 + AI 프록시**까지입니다. 상용화하려면 다음이 추가로 필요합니다.

| 항목 | 현재 상태 | 필요한 작업 |
|---|---|---|
| 결제 | Stripe Checkout + 갱신·해지 웹훅 구현 완료 | 프로덕션 키 발급, 토스페이먼츠 추가 |
| 남용 방지 | 없음 | 레이트 리밋 + 이메일 인증 (가입 자동화로 무료 크레딧 무한 수급 가능 — 렌더 API 실비 손실) |
| 상품 매칭 | 정적 카탈로그. **렌더의 가구와 링크의 제품은 다른 물건**이며 UI·내보내기에 그렇게 고지합니다 | 렌더 이미지에서 가구를 검출해 카탈로그와 임베딩 매칭 |
| 계정 | 이메일+비밀번호, 서버 크레딧 원장 | 이메일 인증, 비밀번호 재설정, 소셜 로그인, 요청 rate limit |
| DB | SQLite 파일 1개 | Postgres 이전 (쿼리가 server/*.js 밖으로 새지 않게 유지해 뒀습니다) |
| 월간 크레딧 갱신 | `invoice.payment_succeeded` 로 매 주기 지급 (같은 인보이스 재지급 차단) | Stripe 프로덕션 웹훅 등록 |
| 구매 전환 데이터 | 클릭까지만 실측 | 각 제휴 콘솔의 정산 API 연동 (클릭→구매 전환율 실측) |
| 제휴 링크 | 검색 URL + 프로그램 추적 파라미터 | 각 파트너 API로 정식 추적 링크 발급 (`lib/affiliate.ts` 의 `buildDeeplink` 만 교체) |
| 검색 URL | 몰별 하드코딩 | 몰이 검색 경로를 개편하면 `MALLS` 의 해당 `searchUrl` 한 줄만 수정 |
| 커미션 요율 | 참고용 구간 | 프로그램 콘솔 승인 후 실제 요율로 교체 |
| 가격 데이터 | 정적 카탈로그(참고용 정가) | 파트너 상품 API 연동 및 주기적 갱신 |
| 렌더 저장 | 브라우저 메모리 | 오브젝트 스토리지 + 히스토리 |

### 법적 체크리스트 (한국 기준)

- **제휴 링크 대가성 표기 의무** — 공정거래위원회 「추천·보증 등에 관한 표시·광고 심사지침」.
  내보내기 결과물에는 문구가 자동 포함되지만, 앱 내 노출 지면에도 표기가 필요합니다.
- **쿠팡 파트너스** 는 지정 문구를 요구하며, 링크 가공 방식에 제약이 있습니다. 가입 승인 후
  약관을 확인하고 `buildDeeplink` 를 정식 API 방식으로 교체하세요.
- **가구 브랜드명·제품 이미지** 사용은 상표/저작권 이슈가 있습니다. 현재 카탈로그는 텍스트와
  색상 스와치만 사용하며 브랜드 이미지를 포함하지 않습니다.
- **AI 생성 이미지 고지** — 렌더 결과는 실제 시공 결과와 다르며, 구조·전기·배관 변경은 전문가
  검토가 필요하다는 안내가 푸터에 포함되어 있습니다.

---

## 단위 경제성

렌더 API 단가는 공급자 정책에 따라 바뀌므로 코드에 상수로 박지 않았습니다.
대신 계산기를 두어, 실제 단가를 넣으면 플랜별 마진이 바로 나옵니다.

```bash
node scripts/unit-economics.mjs <렌더단가USD> <챗단가USD> <소진율>
node scripts/unit-economics.mjs 0.04 0.02 1
```

플랜·크레딧 값은 `src/data/plans.ts` 를 직접 읽으므로, 가격을 바꾸면 계산도 따라옵니다.

**손익분기 렌더 단가** (크레딧을 전부 렌더에 쓴다고 가정한 최악의 경우):

| 상품 | 매출 | 렌더 수 | 분기 단가 |
|---|---|---|---|
| Creator | $19 | 40회 | **$0.47** |
| Pro Creator | $49 | 120회 | **$0.41** |
| Studio | $149 | 480회 | **$0.31** |
| 크레딧 1,150개 팩 | $69 | 230회 | **$0.30** |

가장 빡빡한 상품이 $0.30입니다. 렌더 단가가 이를 넘으면 **헤비 유저에게 팔수록 손해**입니다.

**챗 원가 참고** — Claude Opus 5 기준 입력 약 1,300토큰 / 출력 약 550토큰이면
턴당 약 $0.02입니다. (Opus 5 $5/$25 per MTok, claude-api 스킬 캐시 2026-06-24 기준.
확정 단가는 anthropic.com/pricing 에서 확인하세요.) 챗은 1크레딧이라 어느 플랜에서든
마진이 남습니다 — 관리해야 할 건 렌더입니다.

**Free 플랜은 순수 비용입니다.** 가입 1건당 렌더 4회. 레이트 리밋과 이메일 인증이
없으면 자동 가입으로 이 비용에 상한이 없습니다.

## 스크립트

| 명령 | 설명 |
|---|---|
| `npm run dev` | 개발 서버 (5173) |
| `npm run build` | 타입 체크 + 프로덕션 빌드 |
| `npm run preview` | 빌드 결과 미리보기 |
| `npm run server` | AI 프록시 서버 (8787) |
| `node smoke.mjs a.png b.png` | Playwright 엔드투엔드 스모크 테스트 |
| `node scripts/unit-economics.mjs` | 렌더 단가별 플랜 마진·손익분기 계산 |
