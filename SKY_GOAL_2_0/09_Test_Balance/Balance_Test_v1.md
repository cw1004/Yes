# SKY GOAL 2.0 테스트 / 밸런스 v1.0

## 실행 방법
```bash
./test.sh                    # 빌드 + 엔진 테스트 + 브라우저 스모크 테스트
node --test 07_MVP_Code/tests/*.test.js
node 07_MVP_Code/tests/browser.smoke.mjs --screenshot ./shots
```
브라우저 스모크 테스트는 Playwright 가 설치된 환경에서만 실행되고, 없으면 자동으로 건너뛴다.

## 1. 엔진 단위 테스트 (`tests/engine.test.js`, 32개)
| 항목 | 확인 내용 |
|---|---|
| clamp / norm | 범위를 벗어난 값과 NaN 처리 |
| normalizeProfile | 손상된 저장 데이터 복구 |
| 변동계수 | 표본 부족 시 기본값 |
| 숙련도 | 70/30 지수 평활, 0~100 유지 |
| 난이도 | 한 판당 ±5 제한, 10~95 범위 |
| 스테이지 | 진행도 + 난이도 가속 매핑 |
| 경기장 파라미터 | 난이도↑ → gap↓ / speed↑, 하한·상한 유지 |
| 점수·보상 | 퍼펙트 보너스, 코인, XP, 레벨 요구치 |
| 아이템 | 확률 합 100%, Legendary 5~12% |
| 레벨업 | 스탯 포인트 지급과 소비 |
| commitRun | 지표·연속 기록·최근 10게임 갱신 |
| 시뮬레이션 | 120게임 동안 난이도 수렴과 발산 없음 |
| 저장소 | localStorage 차단·손상 시 폴백 |
| 조작 설정 | 하한(공 20 / 스피드 30) 클램프, 잘못된 값 복구 |
| 공 상하 조정 | 반응 속도만 바뀌고 **점프 높이는 불변** |
| 스피드 조정 | 스크롤 속도만 바뀌고 중력·간격은 불변 |
| 골문 흔들림 | 모든 난이도에서 8~34px 유지, 난이도 비례 |
| 공 5종 | id·무게 중복 없음, 값 범위, 없는 id 는 기본 공 |
| 무게 물리 | 가벼울수록 높이 뜨고 체공이 길다 (차이 15% 이상) |
| 공 구매 | 코인 부족 시 실패하고 차감 없음, 구매 즉시 장착, 중복 구매 차단 |
| 공 데이터 복구 | 없는 id 제거, 미보유 선택은 기본 공으로 |
| 골든볼 | 코인 획득 +15% |
| 완주 보상 | 처음엔 골든볼(즉시 장착), 다음부터 1000코인 + 레전더리 |
| 프로 모드 물리 | 골문 ×0.88 이하, 속도 ×1.2 이상, 흔들림·퍼펙트 판정까지 확인 |
| 프로 해금 | 200점 또는 완주 1회 전에는 전환 실패 |
| 모드 분리 | 난이도·최고 점수를 모드별로 보관, 전환해도 서로 안 섞임 |
| 구버전 마이그레이션 | 모드 개념 이전 프로필의 기록을 아마추어로 이어받음 |
| 기록 보호 | 모드 전환으로 최고 점수가 내려가지 않음 |
| 프로 보상 | 코인·XP 정확히 1.6배 |
| 완주 횟수 | 손상된 저장값 복구 |

## 1-2. 사운드 테스트 (`tests/audio.test.js`, 5개)
| 항목 | 확인 내용 |
|---|---|
| 효과음 | 오실레이터가 실제로 예약된다 (WebAudio 스텁으로 호출 수 검증) |
| BGM | 시작·정지, 강도 0~3 클램프 |
| 음소거 | 토글과 초기 muted 옵션 |
| 미지원 환경 | AudioContext 가 없으면 모든 호출이 무시된다 |
| 생성 실패 | 생성자가 예외를 던져도 게임이 계속된다 |

## 1-3. 배경 테스트 (`tests/scenery.test.js`, 6개)
| 항목 | 확인 내용 |
|---|---|
| 레이어 순서 | 능선 → 강 → 잔디 순으로 배치 |
| 이음매 | 능선 첫/끝 높이가 같아 무한 반복 시 끊기지 않음 |
| 결정성 | 같은 시드 = 같은 지형, 다른 시드 = 다른 지형 |
| 리사이즈 | 크기가 바뀌어도 같은 산 |
| 7개 스테이지 | 팔레트 존재 + 렌더 호출 발생, 알 수 없는 키도 안전 |
| 초기화 전 호출 | resize 전에 draw 해도 죽지 않음 |

## 1-4. 광고판 테스트 (`tests/sponsor.test.js`, 11개)
| 항목 | 확인 내용 |
|---|---|
| 목록 유효성 | id 중복 없음, 색 형식, 문구 길이, 판매용 지면 존재 |
| **클릭 불가** | `CLICKABLE === false` 고정 — 바꾸면 테스트가 막는다 |
| 순환 | 음수·소수·범위 밖 인덱스도 안전 |
| 한글 판별 | 한글/영문/힌디어 구분 |
| 자리 부족 | 배너 높이가 70px 미만이면 그리지 않는다 |
| 세로쓰기 | 한글은 글자 단위로, 영문은 눕혀서 한 번에 |
| 롤링 보드 | 클립 처리, 화면을 채우도록 반복 |
| 가상 브랜드 | 이름 있는 광고는 전부 `fictional: true` (실존 상표 사용 방지) |
| 로고 마크 | 정의된 7종만 사용, 알 수 없는 값도 안전하게 그려짐 |
| 노출 순서 | 모든 보드가 정확히 한 번, **광고 3연속 금지**, 캠페인이 고르게 분포 |
| 대형 세로 광고판 | 높이가 모자라면 생략, 한글 세로쓰기 / 영문 눕히기 |

## 2. 브라우저 스모크 테스트 (`tests/browser.smoke.mjs`, 69개)
실제 Chromium 에서 빌드된 단일 HTML 을 띄워 확인한다.
- 시작 화면 표시 / 엔진·사운드·배경 모듈 로드
- 배경 레이어 순서(산 → 강 → 잔디)
- 실제 클릭으로 AudioContext 가 열리고 BGM 이 재생됨
- 음소거 버튼 토글과 새로고침 후 설정 유지
- 모드: 처음엔 프로 잠김 → 눌러도 안 바뀜 → 조건 충족 후 전환 → **골문이 실제로 좁아짐**,
  난이도 하한 상승, 아마추어 기록 보존, 되돌리기
- 완주 세리머니: 마지막 스테이지 도달 → 헹가래(선수가 실제로 떠오름) → 선물이 프로필에 반영 →
  건너뛰기 후 **경기가 이어지고 점수가 유지됨** → 결과 화면에 🏆 완주 표시
- 경기장 광고판: 목록 로드, **광고판 위를 탭해도 광고가 아니라 공이 반응**(클릭 영역 없음)
- 사이드라인 응원단: 3명 이상 배치, **기준선이 잔디선보다 아래**(플레이 영역 밖),
  킥오프 순간 환호와 색종이 발생, 그러면서 점수는 변하지 않음
- 킥오프: 공이 잔디에서 출발 → 차인 뒤 플레이 위치(±2px)에 정확히 안착 → 탭으로 건너뛰기
- 공 상점: 5종 표시, 코인 부족 시 잠금, 구매·장착·중력 변화·새로고침 유지
- 조작 설정 슬라이더(하한 20/30) 즉시 반영·저장·기본값 복원
- 보상형 광고 이어하기: 점수 유지·콤보 초기화·한 판 1회·광고 실패 시 결과로
- PWA 매니페스트와 서비스 워커
- 경기 시작 → `ready` 상태 → HUD 표시
- 자동 조종 봇이 골문을 연속 통과해 점수를 낸다(최대 3판 시도)
- 결과 화면과 보상 표시
- 프로필 저장, 새로고침 후 복구
- 결과 화면에서 재시작
- 리사이즈 후 렌더 유지, 가로 스크롤 없음
- **콘솔 에러 0건**

## 3. 수동 확인 체크리스트
1. 신규 플레이어가 10판 내에 난이도 10~40 구간으로 수렴하는가
2. 숙련 플레이어의 난이도가 60~90 까지 점진 상승하는가
3. 3연속 실패 후 난이도가 급증하지 않는가 (한 판 ±5)
4. 최근 10게임 성공률이 65~80% 근처를 유지하는가
5. 모바일 터치 / PC 스페이스 / 클릭 모두 동작하는가
6. 탭 전환 후 돌아왔을 때 공이 갑자기 죽지 않는가 (자동 일시정지)
6-1. 스테이지가 바뀔 때 배경과 BGM 강도가 함께 바뀌는가
7. 장시간 플레이 시 골문 배열이 끊기지 않는가

## 4. 목표 KPI
- 첫 3분 내 재시작률 > 60%
- 평균 연속 플레이 시간 > 2분
- 실패 직전 체감 난이도 급상승 최소화
